# Environment & secrets
.env
.env.*
!.env.example

algoritma.md

# Secret/key file formats
*.pem
*.key
*.pfx
credentials.json
*.session.json
id_rsa*

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/

# RL model weights & checkpoints
data/models/
*.pt
*.pth
*.onnx
*.pkl
*.joblib

# Trade data (local only)
data/journal.db
data/trades.db
data/trades.db.*
data/kline_cache.db
data/screenshots/

# SQLite helper files (crash-time artifacts)
*.sqlite
*.sqlite3
*.db-journal
*.db-wal
*.db-shm

# Backups
*.bak
*.backup
*.backup_*

# Data exports
data/*.csv
data/*.parquet
data/exports/

# Arkham on-chain WS stream id cache (gitignored — reused across restarts
# to avoid the v1 session-creation fee; contents are an API-key-scoped id)
data/arkham_stream_id.txt
data/arkham_stream_id.txt.filter

# Pass 1 / Pass 2 analysis + tune report outputs (local artefacts; commit the
# scripts, not the generated reports).
reports/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Test / lint cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml

# Personal config overrides
config/local.yaml
config/*.local.yaml
config/pairs/*.local.yaml

# Privacy — operator-tuned strategy artefacts (see config/default.example.yaml
# for a sanitised public starter). The real tuned configuration, internal
# project notes, Pine indicators, RL/tune analysis scripts, and behaviour
# tests are kept local only — clone-and-run is supported via the .example.yaml.
CLAUDE.md
config/default.yaml
pine/
tests/
scripts/tune_confluence.py
scripts/analyze.py
scripts/factor_audit.py
scripts/replay_decisions.py
scripts/counterfactual_h3_trajectory.py
scripts/counterfactual_max_profit.py
scripts/peg_rejected_outcomes.py
scripts/backfill_proposed_sl_tp.py
scripts/prewarm_kline_cache.py

# OS
.DS_Store
Thumbs.db
desktop.ini

# Node
node_modules/

# Logs
*.log
logs/

# Claude Code memory (personal)
.claude/
